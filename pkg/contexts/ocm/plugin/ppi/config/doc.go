// The config package can be used if the plugin should provide
// the ocm configuration from the calling OCM library.
// Just add an anonymous import to this package to your main program
// or CLI extension command.
// The config is only passed, if a CLI extension caommand is called.
package config
