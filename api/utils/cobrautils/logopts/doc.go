// Package logopts is used for CLI options used to control the logging, globally or for
// a dedicated context.
// If used the main program should call logopts.CloseLogFiles() before exiting.
package logopts
